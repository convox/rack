#!/bin/sh

echo "ns=kernel at=startup pull=\"convox/build:release\""
docker pull convox/build &

if [ "$DEVELOPMENT" == "true" ]; then
  $GOPATH/bin/rerun -build github.com/convox/kernel
else
  $GOPATH/bin/kernel
fi
