#!/bin/sh

echo "ns=kernel at=startup pull=\"convox/builder:release\""
docker pull convox/builder:release &

if [ "$DEVELOPMENT" == "true" ]; then
  $GOPATH/bin/rerun -build github.com/convox/kernel
else
  $GOPATH/bin/web
fi
