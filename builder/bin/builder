#!/bin/bash

root=$(dirname $(dirname $0))

echo "updating builder"
$root/bin/start-docker &
sleep 1
docker pull convox/builder:latest &

if [ "$DEVELOPMENT" == "true" ]; then
  $GOPATH/bin/rerun -build github.com/convox/kernel/builder
else
  $GOPATH/bin/builder
fi
