web:
  build: .
  environment:
    - DEVELOPMENT=true
    - AWS_ACCESS
    - AWS_SECRET
    - AWS_REGION
  ports:
    - 5000:3000
  volumes:
    - .:/go/src/github.com/convox/kernel
    - /var/run/docker.sock:/var/run/docker.sock
