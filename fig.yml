web:
  build: web
  environment:
    - DEVELOPMENT=true
    - AWS_ACCESS
    - AWS_SECRET
    - AWS_REGION
  ports:
    - 5000:3000
  volumes:
    - web:/go/src/github.com/convox/kernel/web
    - /var/run/docker.sock:/var/run/docker.sock
