apiVersion: convox.com/v2
kind: Atom
metadata:
  namespace: {{.Namespace}}
  name: {{.Name}}
spec:
  currentVersion: {{.Name}}-{{.Version}}
  current:
    template: {{.Template}}
    version: "{{.Version}}"
  progressDeadlineSeconds: {{.Timeout}}
status: Pending
